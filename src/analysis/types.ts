/**
 * Analysis-specific types (re-exported from main types for convenience).
 */

export type {
  Endpoint,
  EndpointParam,
  AccessEntry,
  AccessResult,
  AccessFinding,
  InjectionPoint,
  InjectionType,
  CapturedRequest,
} from "../types/index.js";
